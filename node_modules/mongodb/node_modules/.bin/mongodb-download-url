#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mongodb-download-url/bin/mongodb-download-url.js" "$@"
  ret=$?
else 
  node  "$basedir/../mongodb-download-url/bin/mongodb-download-url.js" "$@"
  ret=$?
fi
exit $ret
